"use strict";

// What is use strict in JavaScript medium?
// Strict Mode is a feature in JavaScript that was introduced in ECMAScript 5. It eliminates some JavaScript silent errors by changing them to throw errors. Strict mode applies to entire scripts or to individual functions. It doesn't apply to block statements enclosed in {} braces.

// intro to variables

// variables can store some information
// we can use that information later
// we can change that information later

console.log("hello world");

var firstName="omkar";
console.log(firstName);

firstName="talekar";
console.log(firstName);